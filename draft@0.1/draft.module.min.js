(function (window) {

    "use strict"

    function draftModule () {
        console.log('hello from draftModule!')
    }

    if (typeof(window.draftModule) == 'undefined') {
        window.draftModule = draftModule()
    }

})(window)